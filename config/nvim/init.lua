require("options")
require("keys")
require("autocommands")
require("templates")

require("plugins")

require("plugin_config")

-- Colors at the end so the config is loeaded firts
require("colorscheme")
